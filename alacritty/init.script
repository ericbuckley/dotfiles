#!/bin/sh

# alacritty themes
[ -d "${HOME}/.config/alacritty/themes" ] && rm -rf "${HOME}/.config/alacritty/themes"
git clone --depth 1 --quiet https://github.com/alacritty/alacritty-theme ~/.config/alacritty/themes
